import React from 'react';
import styles from './../styles/HabitTracker.module.scss';

export const NoHabitsForToday = () => {
  return <h1 className={styles.noHabitsForToday}>no habits for today</h1>;
};
